; Copy this file to db.ini and fill in the real values.
; db.ini is gitignored and must NEVER be committed — it holds live DB credentials.
; Read by connect() in config.php via parse_ini_file().

dbhost=localhost
dbuser=your_db_user
dbpass=your_db_password
dbname=your_db_name
